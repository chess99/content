# 内容资产库说明

## 基本原则

- 主语言使用中文。
- `topics/` 只保存准备发布或已经发布的具体内容主题；内部计划、账号策略、品牌资产和跨主题调研不放入 `topics/`。
- 当前生效的账号定位、命名、设计规范和正式共享品牌资产统一放在根目录 `brand/`。
- 过程性调研、对标、候选方案和跨主题研究放在 `research/YYYYMMDD-<slug>/`；日期取首次立项日期，后续更新不改目录名。
- 一个主题一个目录，路径为 `topics/YYYYMMDD-<slug>/`，日期取首次立项日期。
- 不提前创建空渠道目录。
- `topic.md` 记录主题定位、渠道状态、发布日期和链接。

## 共用与渠道版本

- 主题是资产单位，平台只是发布记录。同一文案或素材发到多个平台时，只保存一份。
- 主题根目录只放主题级说明、研究和跨形态共用素材；具体产物按 `longform/`、`image-post/`、`video/` 三种内容形态组织。
- 只创建真实存在的形态目录，不为“以后可能会做”提前建空目录。
- `longform/` 保存博客、公众号文章和小红书长文等长文稿件、配图及其渠道差异。
- `image-post/` 保存小红书、抖音图文和公众号贴图等图片轮播的正文、图片方案与最终图片。
- `video/` 保存视频 brief、脚本、分镜、字幕、封面、发布文案及需要版本管理的成片。
- 跨形态研究、信源和原始素材放在主题根目录的 `research.md` 或 `shared/`。
- 根目录 `research/` 保存不从属于某一篇内容、可能被多个主题复用的调研；每个调研项目至少包含说明目的、状态、结论和转化去向的 `README.md`。
- 小红书和抖音仅有标题、话题等轻微差异时，写入 `topic.md`，不要复制全文。
- 只有内容发生实质改写，或出现平台专属封面、脚本、字幕和特殊尺寸素材时，才在对应形态下创建 `xiaohongshu/`、`wechat/`、`douyin/` 或 `blog/`。
- 渠道目录只保存相对通用版本发生变化的产物，不复制整套主题。
- 不保存仅为传输方便生成的 ZIP；压缩包应从已入库文件按需重新生成。

`topic.md` 中使用发布记录表：

```markdown
| 渠道 | 状态 | 发布时间 | 使用版本 | 链接 |
|---|---|---|---|---|
| 小红书 | 已发布 | 2026-07-23 | `image-post/post.md` + `image-post/images/` | 待补 |
| 抖音 | 已发布 | 2026-07-23 | `image-post/post.md` + `image-post/images/` | 待补 |
```

## 编辑边界

- 本仓库是创作资产的唯一来源。
- `brand/BRAND.md` 是工作流默认读取的账号品牌唯一入口；`brand/` 只保存已正式启用的规则和资产。
- `research/` 不得被创作工作流自动读取。调研结论只有被人工确认并提炼进入 `brand/` 或具体主题后才生效。
- 博客仓库中的文章是发布副本；需要修改时先修改本仓库，再重新同步。
- 不在本仓库开发通用 skill 或工具；相关代码放到 `D:\code\content-studio`。
- 不覆盖原始素材。处理后的图片、音频或视频应保存为新文件。

## 提交要求

- 只提交本次任务涉及的主题和说明文件。
- 提交前检查暂存区，避免混入其他主题的并行修改。
