# 内容资产库说明

## 基本原则

- 主语言使用中文。
- 一个主题一个目录，路径为 `topics/YYYYMMDD-<slug>/`，日期取首次立项日期。
- 不提前创建空渠道目录。
- `topic.md` 记录主题定位、渠道状态、发布日期和链接。

## 共用与渠道版本

- 主题是资产单位，平台只是发布记录。同一文案或素材发到多个平台时，只保存一份。
- 通用正文优先保存为主题根目录的 `post.md`、`article.md` 或 `final.md`；通用图片优先保存到根目录 `images/`。
- 跨渠道研究、信源和原始素材可放在 `shared/`。
- 小红书和抖音仅有标题、话题等轻微差异时，写入 `topic.md`，不要复制全文。
- 只有内容发生实质改写，或出现平台专属封面、脚本、字幕、视频和特殊尺寸素材时，才创建 `xiaohongshu/`、`wechat/`、`douyin/` 或 `blog/`。
- 渠道目录只保存相对通用版本发生变化的产物，不复制整套主题。
- 不保存仅为传输方便生成的 ZIP；压缩包应从已入库文件按需重新生成。

`topic.md` 中使用发布记录表：

```markdown
| 渠道 | 状态 | 发布时间 | 使用版本 | 链接 |
|---|---|---|---|---|
| 小红书 | 已发布 | 2026-07-23 | `post.md` + `images/` | 待补 |
| 抖音 | 已发布 | 2026-07-23 | `post.md` + `images/` | 待补 |
```

## 编辑边界

- 本仓库是创作资产的唯一来源。
- 博客仓库中的文章是发布副本；需要修改时先修改本仓库，再重新同步。
- 不在本仓库开发通用 skill 或工具；相关代码放到 `D:\code\content-workflows`。
- 不覆盖原始素材。处理后的图片、音频或视频应保存为新文件。

## 提交要求

- 只提交本次任务涉及的主题和说明文件。
- 提交前检查暂存区，避免混入其他主题的并行修改。
